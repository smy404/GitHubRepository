<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.my_android">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" /> <!-- To access Google+ APIs: -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.main.数据存储.SFActivity"></activity>
        <activity android:name=".ui.main.数据存储.InnerActivity" />
        <activity android:name=".ui.main.数据存储.SDActivity" />
        <activity android:name=".ui.main.数据存储.NetworkActivity" />
        <activity android:name=".ui.main.数据存储.DBActivity" />
        <activity android:name=".ui.main.数据存储.DataStorageActivity" />
        <activity android:name=".ui.main.xml.XMLActivity" />
        <activity android:name=".ui.main.EmptyActivity" />
        <activity android:name=".ui.main.EventBus.EventBusActivity_2" />
        <activity android:name=".ui.main.EventBus.EventBusActivity_1" />
        <activity android:name=".ui.main.EventBus.EventBusActivity_3" />
        <activity android:name=".ui.main.EventBus.EventBusActivity" />
        <activity android:name=".ui.main.数据存储.LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>